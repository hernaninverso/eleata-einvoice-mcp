{
  "manifest_version": "0.3",
  "name": "eleata-einvoice-mcp",
  "version": "0.1.1",
  "description": "Validate EU e-invoices (Peppol, XRechnung, Factur-X, UBL, CII) and explain error codes.",
  "author": { "name": "eleata", "url": "https://eleata.io" },
  "homepage": "https://eleata.io",
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/dist/index.js"],
      "env": { "EINVOICE_API_KEY": "${user_config.api_key}" }
    }
  },
  "user_config": {
    "api_key": {
      "type": "string",
      "title": "Eleata API Key",
      "description": "Free API key (200 validations/month) from https://eleata.io/signup/",
      "sensitive": true,
      "required": true
    }
  }
}
